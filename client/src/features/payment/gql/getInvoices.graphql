query getInvoices($editionId: Float!, $status: InvoiceStatus) {
  invoices(editionId: $editionId, status: $status) {
    id
    name
    vendorId
    vendor {
      id
      name
    }
    totalAmount
    note
    author {
      id
    }
    executedAt
    status
    payments {
      id
      budgetLineId
      quantity
      unitPrice
    }
  }
}
