mutation addInvoice(
  $name: String!
  $authorId: String!
  $editionId: Float!
  $note: String
  $payments: [AddInvoicePaymentsInput!]!
  $status: InvoiceStatus!
  $totalAmount: Float!
  $vendorId: Float!
  $withoutTVA: Boolean
) {
  addInvoice(
    name: $name
    editionId: $editionId
    note: $note
    payments: $payments
    status: $status
    totalAmount: $totalAmount
    vendorId: $vendorId
    authorId: $authorId
    withoutTVA: $withoutTVA
  ) {
    id
  }
}
