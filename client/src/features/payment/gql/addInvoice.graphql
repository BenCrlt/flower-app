mutation addInvoice(
  $name: String!
  $authorId: ID!
  $editionId: Float!
  $note: String
  $payments: [AddInvoicePaymentsInput!]!
  $status: InvoiceStatus!
  $totalAmount: Float!
  $vendorId: Float!
) {
  addInvoice(
    name: $name
    editionId: $editionId
    note: $note
    payments: $payments
    status: $status
    totalAmount: $totalAmount
    vendorId: $vendorId
    authorId: $authorId
  ) {
    id
  }
}
