query getOrderOrigin($id: Float!) {
  orderOrigin(id: $id) {
    id
    name
    isPhysical
    budgetLines {
      id
      name
      estimatedUnitPrice
      isFreePrice
      category {
        id
        name
        color
      }
    }
  }
}
