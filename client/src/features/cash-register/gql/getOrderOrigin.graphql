query getOrderOrigin($id: Float!) {
  orderOrigin(id: $id) {
    id
    name
    isPhysical
    budgetLines {
      id
      name
      estimatedUnitPrice
      category {
        id
        name
        color
      }
    }
  }
}
