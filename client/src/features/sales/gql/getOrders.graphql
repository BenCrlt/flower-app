query getOrders($editionId: Float!) {
  orders(editionId: $editionId) {
    id
    totalAmount
    executedAt
    payerFirstName
    payerLastName
    payerEmail
    helloAssoOrderId
    author {
      id
      username
    }
  }
}
